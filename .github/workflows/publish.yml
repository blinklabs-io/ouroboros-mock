# Generated automatically by org-governance-bot. Do not edit manually.
name: "publish"

on:
  push:
    branches:
      - main
    tags:
      - v*.*.*

permissions:
  contents: write

jobs:
  orchestrate:
    uses: blinklabs-io/actions/.github/workflows/reuseable-go-library-release.yml@main
    with:
      pull-go-module: false
